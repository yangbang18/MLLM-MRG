# Added by Yang B.
# TODO: urls

datasets:
  iu_xray_caption: # name of the dataset builder
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]

    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url:
              - 
          storage:
              - iu_xray/train.json
        val:
          url:
              - 
          storage:
              - iu_xray/val.json
        test:
          url:
              - 
          storage:
              - iu_xray/test.json
      images:
        storage: iu_xray/images
