# Added by Yang B. 
# TODO: urls

datasets:
  mimic_cxr_caption: # name of the dataset builder
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]

    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url:
              - 
          storage:
              - mimic_cxr/train.json
        val:
          url:
              - 
          storage:
              - mimic_cxr/val.json
        test:
          url:
              - 
          storage:
              - mimic_cxr/test.json
      images:
        storage: mimic_cxr/images
